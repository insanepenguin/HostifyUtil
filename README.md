Quick Script to add a list of Unifi Devices to a controller via SSH.
Add the IPs of all the devices to the ip_list.txt file, before running the script.
Use the powershell script to install python and then install the paramiko libary.
